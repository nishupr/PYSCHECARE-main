version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.php
    ports:
      - "80:80"
    env_file:
      - .env

  backend:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    env_file:
      - .env
